# 贡献指南

[English](./CONTRIBUTING.md) | 简体中文

这个仓库存放的是可复用的 Codex skill。好的贡献应该具备三个特点：容易发现、容易理解、容易在真实代理工作流中复用。

## 适合贡献什么

通常比较有价值的贡献包括：

- 在 `skills/` 下新增一个 skill
- 为现有 skill 补充更好的参考内容
- 改进中英文仓库文档
- 补充预览图、目录页或仓库组织结构

不要添加与 skill 本身无关的泛泛说明，或堆积额外流程文档。

## Skill 结构要求

每个 skill 至少应符合以下结构：

```text
skills/
└─ skill-name/
   ├─ SKILL.md
   ├─ agents/openai.yaml
   └─ references/        （可选）
```

基本要求：

- 目录名使用小写加连字符形式。
- `SKILL.md` 必须包含带 `name` 和 `description` 的 YAML frontmatter。
- `description` 要清楚说明技能做什么、什么情况下应该触发。
- `agents/openai.yaml` 应包含清晰的显示名、简短说明和默认提示词。
- `SKILL.md` 尽量保持精简，较长案例放进 `references/`。

## 编写建议

- 优先写可执行的实用指导，不要写空泛理论。
- 目标读者是另一个 Codex 实例，不是普通终端用户。
- 触发描述要明确。
- 必要时给出示例提示词，帮助技能被正确调用。
- 尽量使用“做什么 / 优先什么”的祈使式写法。
- 不要在 skill 目录里添加 `CHANGELOG.md`、`NOTES.md` 这类无直接价值的额外文件。

## 双语文档建议

如果改动会影响仓库级别的用户可见内容，优先同步更新：

- `README.md`
- `README.zh.md`

如果某个 skill 明确面向中文工作流，也可以考虑：

- 使用中文优先的 `SKILL.md`
- 在 `references/` 中放中文案例
- 在仓库层面保留英文说明，方便非中文读者理解用途

## 校验建议

在提交 PR 或推送前，建议检查：

1. skill 目录结构是否正确。
2. `SKILL.md` 是否渲染正常，触发描述是否足够具体。
3. 仓库内链接是否都能打开。
4. 预览图在 GitHub 页面上是否显示正常。
5. 如果条件允许，运行本地 skill 校验脚本。

给 Windows 贡献者的说明：

- 上游校验脚本可能会按本地默认编码读取文件。
- UTF-8 的中文文件在 GitHub 上显示正常，但仍可能因为校验脚本假设 `gbk` 而本地报错。
- 遇到这种情况，优先在 PR 中说明限制，而不是把正常文本改成乱码编码。

## 推荐工作流

1. 以小而聚焦的改动集提交。
2. 保证 diff 与当前贡献直接相关。
3. 如果影响可发现性，就同步更新仓库文档。
4. 在可行时做本地校验。
5. 使用清晰的提交信息。

## 提交前检查清单

- 新 skill 的目标是否清楚。
- 触发描述是否具体。
- 仓库文档是否仍与当前 skill 列表一致。
- 新增图片或资源是否放在合理路径。
- 这次改动是否没有引入不必要的杂项文件。

## 许可证

提交代码或文档即表示你同意这些贡献按仓库的 MIT 许可证发布。
